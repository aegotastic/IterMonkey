# 🧰 listtools

A collection of nifty Python utilities for manipulating lists, sets, dictionaries, and simple networks — including sorting algorithms, correlation metrics, and pathfinding helpers.

## Installation

```bash
pip install listtools
```